
import 'package:flutter/material.dart';
class AddLocation extends StatelessWidget {
  static String id = 'add_location';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('im add location'),
    );
  }
}